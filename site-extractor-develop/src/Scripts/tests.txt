scanf("Quel est votre age ? %d, %d", &age, &coucou);



while [ $m -lt $j ]; do
  if [ ${line:l:1} = $par ]; then
    echo ${line:$(($i+1)):$(($m-1))}
    m=$j
    i=$m
  fi
  if [ ${line:l:1} = $fin ]; then
    echo ${line:$(($i+1)):$(($m-1))}
    m=$j
    i=$m
  fi
  let m++
  let l++
done
